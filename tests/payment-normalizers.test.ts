import test from "node:test";import assert from "node:assert/strict";import {normalizeTronTransfer,normalizeBaseUsdcTransfer} from "../packages/payments/src/index.ts";
test("normalises confirmed TronGrid TRC20 transfer",()=>{const x=normalizeTronTransfer({transaction_id:"tron1",block_timestamp:10,from:"Ta",to:"Tb",type:"Transfer",value:"100000000",token_info:{address:"Tusdt",decimals:6}},20);assert.equal(x.network,"TRON_TRC20");assert.equal(x.amountMinor,100000000n)});
test("normalises Base USDC Transfer log",()=>{const addr=(x:string)=>"0x"+x.padStart(40,"0");const topic=(x:string)=>"0x"+x.slice(2).padStart(64,"0");const recipient=addr("abc");const x=normalizeBaseUsdcTransfer({transactionHash:"0x1",status:"0x1",blockNumber:"0x10",logs:[{address:addr("8335"),topics:["0xddf252ad1be2c89b69c2b068fc378daa952ba7f163c4a11628f55a4df523b3ef",topic(addr("111")),topic(recipient)],data:"0x5f5e100"}]},{chainId:8453,expectedChainId:8453,confirmations:12,timestamp:10,recipient,tokenContract:addr("8335")});assert.equal(x.amountMinor,100000000n);assert.equal(x.to.toLowerCase(),recipient.toLowerCase())});
test("Base normaliser rejects wrong chain and missing log",()=>{assert.throws(()=>normalizeBaseUsdcTransfer({transactionHash:"x",status:"0x1",blockNumber:"0x1",logs:[]},{chainId:1,expectedChainId:8453,confirmations:1,timestamp:1,recipient:"0x0",tokenContract:"0x1"}),/WRONG_BASE_CHAIN/)});
