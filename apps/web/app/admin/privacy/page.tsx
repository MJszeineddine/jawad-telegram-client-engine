import { redirect } from "next/navigation";
import { currentAdmin,loadPrivacyRequests } from "../../../lib/admin";
export const dynamic="force-dynamic";
export default async function PrivacyAdminPage(){const admin=await currentAdmin();if(!admin)redirect("/admin/login");const data=await loadPrivacyRequests();return <main className="admin-shell"><a href="/admin">← Dashboard</a><header className="admin-top"><div><span>PRIVACY OPERATIONS</span><h1>Deletion requests</h1><p>Source: {data.source}. Completion removes stored attachments and direct Telegram identity, redacts conversations/testimonials, and minimises encrypted intake while retaining only necessary accounting and audit records.</p></div></header>{data.error?<div className="alert">Degraded mode: {data.error}</div>:null}<section className="panel"><div className="panel-head"><h2>Requests</h2><span>{data.requests.length}</span></div><div className="table-wrap"><table><thead><tr><th>Requested</th><th>Identity</th><th>Status</th><th>Notes</th><th>Action</th></tr></thead><tbody>{data.requests.map(request=><tr key={request.id}><td>{new Date(request.requestedAt).toLocaleString("en-GB")}</td><td>{request.telegramUserId?`Telegram ${request.telegramUserId}`:request.email??"Manual verification required"}</td><td>{request.status}</td><td>{request.notes??"—"}</td><td>{request.status==="open"&&request.telegramUserId?<form method="post" action={`/api/admin/privacy/deletions/${request.id}/complete`}><input type="hidden" name="csrf" value={admin.csrf}/><label className="check"><input type="checkbox" name="acknowledged" required/>Identity and scope verified</label><button className="danger">Complete deletion</button></form>:request.status==="open"?"Verify identity manually":"Completed"}</td></tr>)}</tbody></table></div></section><section className="panel"><h2>Retention boundary</h2><p>Payment assignment and audit evidence are retained in minimised form where operationally necessary. The system never stores wallet private keys or seed phrases, so no such material is included in deletion or backup work.</p></section></main>}
